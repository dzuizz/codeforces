#include <iostream>
using namespace std;
typedef long long ll;

int n, m, smallsum=0;

int main(){
    cin >> n >> m;
    int a[n], b[n], diff[n];
    for(int i=0;i<n;i++){
        cin >> a[i] >> b[i];
        diff[i] = a[i]-b[i];
        sum += b[i];
    }
    if(sum>=m){
        while()
    }
}